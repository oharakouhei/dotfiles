## About Repository
Dotfiles, setting files like .vimrc, .zshrc, ...

## How to install
```
$ cd
$ git clone git@github.com:oharakouhei/dotfiles.git
$ sh ./dotfiles/install.sh
$ vi # install plugins by refering to ~/dotfiles/_vimrc
and some sentences will be appeared. so, input "y(es)".
```
